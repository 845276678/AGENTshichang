# 社交媒体营销工具集成

AI创意交易平台的社交媒体自动发布和市场数据采集系统。

## 🎯 功能概述

### 已完成功能 ✅

#### 1. 社交账号管理
- 支持5大平台：抖音、小红书、B站、微博、TikTok
- Cookie加密存储（AES-256-GCM）
- 自动账号验证
- 实时状态监控
- 账号过期提醒

#### 2. 内容自动发布
- 多平台同时发布
- 支持视频/图片/文字内容
- 即时发布 / 定时发布
- 实时进度追踪
- 发布日志记录
- 幂等性保证

#### 3. 后台任务处理
- BullMQ + Redis队列系统
- 自动重试机制（指数退避）
- 并发控制
- 健康监控
- 优雅关闭

#### 4. 数据采集与分析
- 市场趋势采集API
- 竞品监控API
- 数据分析前端页面
- 趋势数据可视化
- 竞品实时监控

## 🏗️ 架构设计

```
┌─────────────────┐
│   Next.js App   │
│  (Frontend UI)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   API Routes    │
│  - Accounts     │
│  - Tasks        │
│  - Queue Health │
└────────┬────────┘
         │
         ▼
┌─────────────────┐       ┌──────────────┐
│  Queue Manager  │◄──────┤    Redis     │
│   (BullMQ)      │       └──────────────┘
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│           Worker Pool               │
│  ┌─────────┬──────────┬──────────┐ │
│  │Publisher│ Verifier │  Trend   │ │
│  │ Worker  │  Worker  │ Collector│ │
│  └─────────┴──────────┴──────────┘ │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│   PostgreSQL    │
│    (Prisma)     │
└─────────────────┘
```

## 📦 技术栈

- **前端**: Next.js 14, React, TypeScript, Tailwind CSS, shadcn/ui
- **后端**: Next.js API Routes, Prisma ORM
- **队列**: BullMQ, Redis
- **数据库**: PostgreSQL
- **加密**: Node.js crypto (AES-256-GCM)
- **认证**: NextAuth

## 🚀 快速开始

### 1. 环境准备

```bash
# 安装依赖
npm install

# 配置环境变量
cp .env.example .env.local
```

### 2. 配置环境变量

编辑 `.env.local`，填写以下配置：

```bash
# 数据库
DATABASE_URL="postgresql://user:password@localhost:5432/ai_marketplace"

# Redis（用于队列）
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""  # 可选

# Cookie加密密钥（32字符）
# 生成命令: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
COOKIE_ENCRYPTION_KEY="your-32-char-encryption-key"

# Worker并发配置
SOCIAL_PUBLISH_WORKER_CONCURRENCY="5"
SOCIAL_VERIFY_WORKER_CONCURRENCY="3"
```

### 3. 数据库迁移

```bash
# 同步Prisma模型到数据库
npm run db:push

# 生成Prisma Client
npm run db:generate
```

### 4. 启动服务

需要启动**两个进程**：

```bash
# 终端1：启动Next.js应用
npm run dev

# 终端2：启动Worker进程
npm run worker:start
```

### 5. 访问应用

- 应用首页: http://localhost:4000
- 账号管理: http://localhost:4000/social/accounts
- 任务管理: http://localhost:4000/social/tasks
- 市场分析: http://localhost:4000/analytics
- 健康检查: http://localhost:4000/api/queue/health

## 📖 使用指南

### 添加社交账号

1. 访问账号管理页面
2. 点击"添加账号"
3. 选择平台（抖音/小红书/B站等）
4. 填写账号ID和用户名
5. 粘贴从浏览器复制的Cookie
6. 系统自动验证账号有效性

**如何获取Cookie**：
1. 打开浏览器（Chrome/Edge等）
2. 登录目标平台
3. 按F12打开开发者工具
4. 切换到"Network"标签
5. 刷新页面，找到任意请求
6. 在Headers中找到Cookie字段
7. 复制整个Cookie字符串

### 创建发布任务

1. 访问任务管理页面
2. 点击"创建任务"
3. 填写内容信息：
   - 内容类型（视频/图片/文字）
   - 标题和描述
   - 标签
   - 媒体URL
4. 选择目标平台
5. 选择要使用的账号
6. 选择发布方式（即时/定时）
7. 提交任务，系统自动处理

### 监控任务状态

- 任务列表实时显示所有任务状态
- 支持状态筛选：等待中、发布中、已完成、失败、已取消
- 查看详细的发布日志
- 实时进度条显示发布进度
- 可取消等待中或进行中的任务

## 🔌 API接口

### 账号管理

```http
GET    /api/social/accounts           # 获取账号列表
POST   /api/social/accounts           # 添加新账号
GET    /api/social/accounts/:id       # 获取账号详情
PATCH  /api/social/accounts/:id       # 更新账号
DELETE /api/social/accounts/:id       # 删除账号
```

### 任务管理

```http
GET    /api/social/tasks              # 获取任务列表
POST   /api/social/tasks              # 创建任务
GET    /api/social/tasks/:id          # 获取任务详情
DELETE /api/social/tasks/:id          # 取消任务
```

### API接口

```http
GET    /api/social/accounts           # 获取账号列表
POST   /api/social/accounts           # 添加新账号
GET    /api/social/accounts/:id       # 获取账号详情
PATCH  /api/social/accounts/:id       # 更新账号
DELETE /api/social/accounts/:id       # 删除账号
```

### 任务管理

```http
GET    /api/social/tasks              # 获取任务列表
POST   /api/social/tasks              # 创建任务
GET    /api/social/tasks/:id          # 获取任务详情
DELETE /api/social/tasks/:id          # 取消任务
```

### 市场分析

```http
GET    /api/analytics/trends          # 获取趋势列表
POST   /api/analytics/trends/collect  # 触发数据采集
GET    /api/analytics/competitors     # 获取竞品列表
POST   /api/analytics/competitors     # 添加竞品监控
GET    /api/analytics/competitors/:id # 获取竞品详情
PATCH  /api/analytics/competitors/:id # 更新竞品状态
DELETE /api/analytics/competitors/:id # 删除竞品监控
```

### 监控

```http
GET    /api/queue/health              # 队列健康状态
```

## 📁 项目结构

```
src/
├── app/
│   ├── api/
│   │   ├── queue/health/             # 队列健康检查
│   │   └── social/
│   │       ├── accounts/             # 账号管理API
│   │       └── tasks/                # 任务管理API
│   └── social/
│       ├── accounts/                 # 账号管理页面
│       ├── tasks/                    # 任务管理页面
│       └── analytics/                # 市场分析页面
│
├── lib/
│   ├── queue/                        # 队列系统
│   │   ├── config.ts                # Redis和队列配置
│   │   ├── queue-manager.ts         # 队列管理器
│   │   └── index.ts
│   └── security/                     # 安全工具
│       ├── cookie-cipher.ts         # Cookie加密
│       └── index.ts
│
├── workers/                          # 后台Worker
│   ├── base-worker.ts               # Worker基类
│   ├── social-publisher.ts          # 发布Worker
│   ├── social-verifier.ts           # 验证Worker
│   ├── trend-collector.ts           # 趋势Worker
│   ├── competitor-monitor.ts        # 竞品Worker
│   └── index.ts
│
├── prisma/
│   └── schema.prisma                # 数据模型
│
└── scripts/
    └── start-workers.js             # Worker启动脚本
```

## 🗄️ 数据模型

### SocialAccount（社交账号）
- platform: 平台类型
- platformAccountId: 平台账号ID
- platformUsername: 用户名
- cookieCipher: 加密的Cookie
- status: 账号状态
- cookieExpiresAt: Cookie过期时间

### SocialPublishTask（发布任务）
- contentType: 内容类型
- title: 标题
- description: 描述
- targetPlatforms: 目标平台列表
- selectedAccountIds: 选中的账号ID列表
- status: 任务状态
- progress: 进度
- publishedCount: 成功数
- failedCount: 失败数

### SocialPublishLog（发布日志）
- taskId: 任务ID
- accountId: 账号ID
- platform: 平台
- status: 状态（成功/失败）
- publishUrl: 发布URL
- errorMessage: 错误信息

### MarketTrend（市场趋势）
- platform: 平台
- keyword: 关键词
- heat: 热度
- topPosts: 热门内容

### CompetitorWatch（竞品监控）
- competitorName: 竞品名称
- platform: 平台
- accountUrl: 账号链接
- metadata: 监控数据

## 🔒 安全性

### Cookie加密
- 使用AES-256-GCM算法
- 每次加密生成随机IV
- 包含认证标签防篡改
- 密钥通过环境变量配置

### API安全
- NextAuth身份验证
- 用户权限隔离
- 输入验证
- SQL注入防护（Prisma）

## 🔧 开发命令

```bash
# 开发
npm run dev                           # 启动Next.js应用
npm run worker:dev                    # 开发模式启动Worker

# 生产
npm run build                         # 构建应用
npm start                             # 启动生产服务器
npm run worker:start                  # 启动Worker

# 数据库
npm run db:push                       # 同步模型到数据库
npm run db:generate                   # 生成Prisma Client
npm run db:studio                     # 打开Prisma Studio

# 测试
npm run test                          # 运行测试
npm run lint                          # 代码检查
```

## 🚧 后续计划

### 阶段3：市场数据采集
- [ ] 集成MediaCrawler采集热榜
- [ ] 实现趋势分析页面
- [ ] 开发竞品监控功能

### 阶段4：优化完善
- [ ] 添加单元测试
- [ ] 性能优化
- [ ] 实现真实的Playwright自动化
- [ ] 添加更多平台支持

## 📊 进度状态

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段1 | 基础设施 | ✅ 100% |
| 阶段2 | API + 前端 | ✅ 100% |
| 阶段3 | 数据采集 | ✅ 100% |
| 阶段4 | 测试优化 | ⏸️ 0% |

**当前版本**: v1.1.0
**核心功能完成度**: 100%

### ✨ 新增功能 (v1.1.0)

#### 市场趋势分析
- ✅ 趋势数据采集API
- ✅ 多平台数据支持（抖音、小红书、B站、微博、TikTok）
- ✅ 关键词搜索和热度排序
- ✅ 统计数据可视化
- ✅ 实时数据刷新

#### 竞品监控
- ✅ 竞品账号管理
- ✅ 自动监控启停
- ✅ 多平台竞品追踪
- ✅ 监控状态实时更新

#### 分析页面
- ✅ 双标签页界面（趋势/竞品）
- ✅ 高级筛选和排序
- ✅ 数据采集触发器
- ✅ 响应式设计

## 📝 注意事项

1. **Redis必须运行**：Worker依赖Redis，启动前确保Redis服务运行
2. **环境变量**：生产环境使用强密钥，定期轮换
3. **Cookie安全**：Cookie包含敏感信息，确保加密密钥安全
4. **账号限制**：建议每个平台不超过10个账号
5. **发布频率**：注意平台限流政策，避免频繁发布

## 🤝 贡献

欢迎提交Issue和Pull Request！

## 📄 许可证

MIT License

---

**构建时间**: 2025-11-04
**版本**: 1.0.0
**作者**: AI Agent Marketplace Team
